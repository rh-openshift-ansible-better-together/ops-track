apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: "{{ project_name }}"
  annotations:
    nekic.io/size: "{{ size }}"
spec:
  hard:
    cpu: "4"
    memory: 8G
    persistentvolumeclaims: "10"

