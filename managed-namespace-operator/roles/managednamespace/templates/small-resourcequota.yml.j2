apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: "{{ project_name }}"
  annotations:
    nekic.io/size: "{{ size }}"
spec:
  hard:
    cpu: "1"
    memory: 1G
    persistentvolumeclaims: "5"

